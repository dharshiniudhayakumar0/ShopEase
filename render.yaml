services:
  - type: web
    name: shopease-master
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: shopease-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATABASE_PATH
        value: /data/shopease.db
